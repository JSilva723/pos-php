<form method="post" action="{{ path(path, {'id': id}) }}" onsubmit="return confirm('{{ confirm_delete|trans }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ id) }}">
    <button class="btn btn-danger w-100">{{ 'Delete'|trans}}</button>
</form>
